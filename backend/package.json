{
  "name": "minilib-backend",
  "version": "1.0.0",
  "description": "API REST Minilib - Node.js / Express",
  "author": "Nif-dev",
  "type": "module",
  "private": true,
  "main": "src/app.js",
  "scripts": {
    "start": "node --env-files=.env src/app.js",
    "dev": "node --watch --env-files=.env.dev src/app.js",
    "test": "echo \"Error: no test specified...YET !\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
