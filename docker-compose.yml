#% docker-compose.yml

services:

  # Database
  db :
    image: postgres:18
    restart: unless-stopped

    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}

    volumes: 
      - postgres_data:/var/lib/postgresql/
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./database/views.sql:/docker-entrypoint-initdb.d/02-views.sql
      - ./database/constraints.sql:/docker-entrypoint-initdb.d/03-constraints.sql
      - ./database/seed.sql:/docker-entrypoint-initdb.d/04-seed.sql

    ports:
      - "5432:5432"

  # Application Backend
  backend:
    build: ./backend
    restart: unless-stopped
    
    environment:
      PORT: 5000
      NODE_ENV: dev
      # DB Connection - nom du service, pas localhost
      DB_HOST: db
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
    
    ports:
      - "5000:5000"

    depends_on:
      - db

  # Application Frontend
  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend

# Volumes - persistance des données 
volumes:
  postgres_data: